{
  "name": "life-optimizers",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "agents/*",
    "shared/*",
    "config"
  ],
  "scripts": {
    "build": "tsc --build",
    "dev": "tsc --build --watch",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0"
  }
}
